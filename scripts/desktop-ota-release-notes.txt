- CodeBench runs with the C++ compiler Setup already installed, instead of saying the compiler is missing.
- Install compiler replaces a compiler that cannot compile, instead of repeating the same check.
