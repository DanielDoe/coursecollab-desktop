#!/usr/bin/env node
/**
 * DEPRECATED — bulk web → desktop sync was removed from package.json.
 * It overwrote Magnific shell, faculty CodeBench, and other desktop-only UI.
 * Port web changes manually instead.
 */

console.error(
  'sync-from-web-app.mjs is disabled. This repo no longer supports npm run sync:web.',
)
process.exit(1)
