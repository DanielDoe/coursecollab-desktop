/**
 * @deprecated Use `sql` from `@/lib/db` (Neon over HTTPS — school / campus Wi‑Fi friendly).
 * This module re-exports the same client for any legacy imports of `@/lib/db-direct`.
 */

export { sql } from "./db"
